/**
 * Android-specific MapScreen implementation
 * Uses WebView with Google Maps JavaScript API instead of react-native-maps
 * to avoid Google Play Services API key configuration issues on Android emulators
 */

export { MapScreen } from './MapScreenSafe';
export { default } from './MapScreenSafe';
